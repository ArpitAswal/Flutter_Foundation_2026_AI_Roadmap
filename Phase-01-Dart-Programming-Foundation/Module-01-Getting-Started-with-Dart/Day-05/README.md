# 📘 Day 5: Functions as First-Class Objects, Callbacks & Closures

> [!NOTE]
> **Summary:** Go deeper into Dart's functional capabilities by understanding how functions can be stored, passed, returned, and used as callbacks and closures in real applications.

**Tags:** `Dart`, `First-Class Functions`, `Callbacks`, `Function Types`, `Anonymous Functions`, `Closures`, `Tear-Offs`

---

## 🚦 Prerequisites
You should understand variables, data types, null safety, operators, control flow, functions, parameters, return values, and basic Flutter callback syntax.

## 📖 Overview
Day 4 introduced how to create and call functions. That is necessary, but it is not enough to understand modern Dart and Flutter code.

Flutter APIs constantly accept functions as values.

Examples include:

## 📚 Topics Covered
* **Why Are We Going Deeper Into Functions?**: Day 4 introduced how to create and call functions. That is necessary, but it is not enough to understand modern Dart and Flutter code.
* **Definition**: A callback is a function supplied to another function, object, or API so that it can be invoked later or at a particular point in an operation.
* **Definition**: A closure is a function that captures variables from its surrounding lexical scope and continues to access those variables when the function is use...
* **Definition**: A tear-off occurs when a function, method, or named constructor is referenced without parentheses so that it can be used as a function value.

## 💡 Additional Materials Included
* **12 Interview Questions** included
* **Common Mistakes & Optimizations** included
* **Architecture Implementation Notes** included

> [!TIP]
> **Deep Dive:** To read the full theory, view detailed code implementations, architectures, and common interview questions, open this lesson interactively inside the **Flutter AI Tutor App**!
